# Core frameworks
flask==3.0.0
flask-cors==4.0.0
requests==2.31.0
python-dotenv==1.0.0

# Voice recognition (use system packages instead)
# speech_recognition==3.10.0  # Comment out - install via apt
# pyaudio==0.2.11            # Comment out - install via apt

# AI integration
openai==1.3.0
anthropic==0.7.0

# Text-to-speech
pyttsx3==2.90

# System utilities
psutil==5.9.6
# ===== RASPBERRY PI SPECIFIC =====
# These may need system-level installation:
# sudo apt-get install espeak-ng
# sudo apt-get install portaudio19-dev
# sudo apt-get install python3-pyaudio

# ===== INSTALLATION =====
